class Solution {
    static String revStr(String S) {
        // code here
        int n = S.length();
        StringBuilder sb = new StringBuilder();
        for(int i=0 ; i<n ; i++){
            sb.append(S.charAt(n-1-i));
        }
        return sb.toString();
        
    }
}
